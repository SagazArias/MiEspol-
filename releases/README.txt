# Releases

Instala `Campus-ESPOL-plus.apk` en Android (permite orígenes desconocidos si hace falta).

La app es standalone: no necesita Expo Go ni el PC. Sí necesita internet para clima (Open-Meteo) y tiles del mapa (OpenStreetMap).
